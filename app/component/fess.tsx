type FessProp = {
    identity : string,
    permission: string // public, private, semi public idk
}

const Fess: React.FC<FessProp> = ({identity}) => {
    return <div className="w-full grid grid-cols-1 md:grid-cols-2 lg:grid-cols-5">

    </div>
}